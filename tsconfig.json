{
  "compilerOptions": {
    "jsx": "react",
    // Enable JSX syntax
    "jsx": "react", // or "preserve", depending on your use case

    // Allow JavaScript files to be compiled
    "allowJs": true,

    // Other options...
  },

  // Include .tsx and .js files
  "include": ["/**/*.tsx", "/**/*.js"]
}