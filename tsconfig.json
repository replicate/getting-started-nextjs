{
  "compilerOptions": {
    // other options...
    "jsx": "react", // enable JS
    // Allow JavaScript files to be compiled
    "allowJs": true,
    "baseUrl": ".", // This must be specified if "paths" is specified
    "paths": {
      "@/*": [".app/*"] // This assumes that your 'components' directory is inside 'src'
    }

    // Other options...
  },
  // Include .tsx and .js files
  "include": [  "./app/**/*.{js,ts,jsx,tsx}",
  "./pages/**/*.{js,ts,jsx,tsx}",
  "./components/**/*.{js,ts,jsx,tsx}" ],
  /** @type {import('tailwindcss').Config} */
 
}